-- 코드를 작성해주세요
SELECT
    COUNT(*) AS FISH_COUNT,
    MONTH
FROM
    FISH_INFO
JOIN (
    SELECT
        ID,
        MONTH(TIME) AS MONTH
    FROM FISH_INFO
) AS MONTH_INFO
ON FISH_INFO.ID = MONTH_INFO.ID
GROUP BY
    MONTH
ORDER BY
    MONTH
